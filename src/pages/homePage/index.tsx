import Filter from 'components/productsFilters';

import { HomeSection } from './styles';

const HomePage = () => {
  return (
    <HomeSection>
      <Filter />
    </HomeSection>
  );
};

export default HomePage;
